@model Fall2025_Project3_krhanson3.Models.Actors

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Actors</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Gender)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Gender)
        </dd>

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Age)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Age)
        </dd>

        <dt class="col-sm-2">
            IMDB
        </dt>
        <dd class="col-sm-10">
            @if (!string.IsNullOrEmpty(Model.IMDBUrl))
            {
                <a href="@Model.IMDBUrl" target="_blank" class="link-primary">IMDB Profile</a>
            }
            else
            {
                <span class="text-muted">Not available</span>
            }
        </dd>

        <dt class="col-sm-2">
            Photo
        </dt>
        <dd class="col-sm-10">
            @if (Model.Photo != null && Model.Photo.Length > 0)
            {
                var imageBase64 = Convert.ToBase64String(Model.Photo);
                var imageSrc = $"data:image/png;base64,{imageBase64}";
                <img src="@imageSrc" alt="Actor Photo" class="img-thumbnail mt-2" style="width:150px; height:150px; object-fit:cover;" />
            }
            else
            {
                <span class="badge bg-secondary">No photo available</span>
            }
        </dd>
    </dl>
</div>

<div class="mt-3">
    <a asp-action="Edit" asp-route-id="@Model.ActorId" class="btn btn-outline-primary">Edit</a>
    <a asp-action="Index" class="btn btn-outline-secondary ms-2">Back to List</a>
</div>
